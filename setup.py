from setuptools import setup

setup(
    name="paradrop",
    version="0.3.0",
    author="Paradrop Labs",
    description="Paradrop wireless virtualization",
    install_requires=[
        'flask'
    ],

    packages=['paradrop'],
)
